@{
    ViewData["Section"] = "Administration Panel";
    ViewData["Subsection"] = "Edit User";
}
@model neobooru.ViewModels.Forms.RoleCheckboxesViewModel

<div class="row">
    <h1>Edit User's Roles</h1>
</div>
<div class="row">
    @* <form method="post" class="col-12"> *@
        @* <div class="form-group row"> *@
            @* <div class="col-sm-10"> *@
@*                 @foreach (var roleCheckbox in Model.Checkboxes) *@
@*                 { *@
@*                 <div class="form-check"> *@
@*                     <input type="checkbox" asp-for="@roleCheckbox.IsChecked" class="form-check-input"> *@
@*                     <label class="form-check-label" asp-for="@roleCheckbox.Role">@roleCheckbox.Role</label> *@
@*                 </div> *@
@*                 } *@
            @* </div> *@
        @* </div> *@

        @* <div asp-validation-summary="All" class="text-danger"></div> *@
        @* <div class="form-group row"> *@
            @* <div class="col-sm-10"> *@
                @* <button type="submit" class="btn btn-success">Update</button> *@
                @* <a asp-action="ListRoles" class="btn btn-danger">Cancel</a> *@
            @* </div> *@
        @* </div> *@
    @* </form> *@
    @using (Html.BeginForm())
    {
        @Html.HiddenFor(m => Model.UserId);
        <div class="col-sm-12">
           @for (int i = 0; i < Model.Checkboxes.Count; i++)
           {
               @Html.CheckBoxFor(m => Model.Checkboxes[i].IsChecked,
                   new { @class = "form-check-input"});
               @Model.Checkboxes[i].RoleName
               @Html.HiddenFor(m => Model.Checkboxes[i].RoleId)
               @Html.HiddenFor(m => Model.Checkboxes[i].RoleName)<br/>
           }
           <input type="submit" value="Go"/>
        </div>
    }
</div>